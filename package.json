{
  "name": "glamour",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "npx tailwindcss build -i src/style.css -o public/style.css",
    "deploy": "npm run build-css && gh-pages -d public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^2.2.4"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}
